'use strict';

module.exports = function(sql) {
/*
	// Add ANSI operators
	sql.registerOperator('select');

	// Add ANSI logical helpers
	sql.registerHelper('/logical/and');

	// Add ANSI comparision helpers
	sql.registerHelper('/comparision/eq');

	// Add ANSI aggregation helpers
	sql.registerHelper('/aggregation/count');

	// Add ANSI functions
	sql.registerHelper('/functions/comparision/cmp');

	// Add ANSI misc helpers
	sql.registerHelper('/functions/misc/isnull');
*/
}
