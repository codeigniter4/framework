import React, { Component } from "react";
import PropTypes from "prop-types";

const Header = (props) => {
  return (
    <div>
      Header
    </div>
  )
}

Header.propTypes = {};

export default Header
